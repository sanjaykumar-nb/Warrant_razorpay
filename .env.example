# GROQ_API_KEY is what this submission's reported numbers come from.
# Free, no card required: https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# Optional fallbacks, only used when GROQ_API_KEY is absent.
# Gemini free tier: https://aistudio.google.com/apikey
GEMINI_API_KEY=...

# Anthropic (paid — needs credit on the account).
ANTHROPIC_API_KEY=sk-ant-...
# Only required if your Anthropic key is workspace-scoped.
ANTHROPIC_WORKSPACE_ID=wrkspc_...
