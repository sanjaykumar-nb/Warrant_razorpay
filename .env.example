# GEMINI_API_KEY is what this submission actually runs on — free, no
# card, get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=...

# Optional fallback if you'd rather use Anthropic instead. Only used when
# GEMINI_API_KEY is absent.
ANTHROPIC_API_KEY=sk-ant-...
# Required if your Anthropic key is workspace-scoped ("identity-linked API key").
ANTHROPIC_WORKSPACE_ID=wrkspc_...
